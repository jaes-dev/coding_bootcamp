// Below is a header guard - basically checking if this header has already been included or not.
// Not necessary for smaller projects like this hence I put only here. Safer to have though, especially in bigger projects.
#ifndef GET_TERMINAL_LENGTH_H
#define GET_TERMINAL_LENGTH_H

// Function to put "input" into the centre of the terminal, surrounded by "----"s
int getTerminalLength();

#endif // GET_TERMINAL_LENGTH_H